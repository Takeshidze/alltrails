name: alltrails
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  carousel_slider: ^4.2.1
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  provider: ^6.1.1
  country_code_picker: ^3.0.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  supabase: ^1.11.11
  fluttertoast: ^8.2.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo_tour_operators/